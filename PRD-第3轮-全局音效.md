# 第 3 轮 · PRD · 全局音效（BGM + SFX）

**课程版本**：B2 · 第 3 轮 / 共 6 轮  
**文档版本**：v1.0  
**日期**：2026-05-29  
**关联文档**：`01-第1轮-PRD-小丑牌核心循环.html`（§5.1 动画时序、§5.4 设置面板）

---

## 1. 一句话定位

为已上线的小丑牌网页游戏接入 Howler.js 音频引擎，让第 1 轮预留的 BGM/SFX 滑块真正生效，并为游戏里每一个关键操作配上对应的音效，使玩家在没有任何视觉提示的情况下也能通过声音感知游戏节奏。

---

## 2. 目标用户与场景

**目标用户**：正在跟着教学课程做这个项目的独立开发者学员，以及最终玩这个游戏的休闲玩家。

**核心场景**：
- 学员在浏览器里调试游戏时，希望有背景音乐陪伴，出牌、计分、通关时有反馈音效，让产品有"做完了"的质感。
- 玩家打牌时，每一步操作（选牌、出牌、Joker 触发、通关）都有声音即时反馈，不需要盯着 HUD 数字确认"这一下有没有生效"。

**当前状态（第 1 轮留下的）**：
- 设置面板里 BGM 音量（默认 50）和 SFX 音量（默认 70）两个滑块已经存在，值也写进了 `localStorage` 的 `balatro.settings` 键里。
- 但这两个滑块只存值，没有接任何音频引擎，拖动后什么都不会发生。
- 游戏所有操作完全静音。

---

## 3. 核心价值主张

第 1 轮做完之后游戏"能玩但没声音"，体验缺一块。本轮补上这块：用最少的接线工作（Howler.js + CC0 素材），把已有的滑块激活，让游戏从"能玩"变成"好玩"。

对学员而言，这一轮的核心收获是：**理解浏览器 Autoplay 策略、音频预加载、音量映射、音效与动画时序对齐**这四个工程问题的处理方式。

---

## 4. 功能清单（MVP）

### P0 · 必须做（核心接线）

**US-01 · 引入 Howler.js**  
作为开发者，我想通过 npm 安装 Howler.js 并在项目里统一初始化，以便后续所有音效共用同一个音频引擎，不用每个组件各自管理 Audio 对象。

**US-02 · BGM 循环播放**  
作为玩家，我想在进入游戏后（首次点击任意交互后）听到一首循环的背景音乐，以便在打牌时有氛围感。  
- BGM 必须在用户首次交互（点击任意按钮/牌）后才开始播放，不能在页面加载时自动播（浏览器 Autoplay 策略硬约束，见第 5 节）。
- BGM 循环播放，玩家切换关卡、进商店、通关/失败全程不中断（通关/失败时可以淡出停止，见 US-07/US-08）。

**US-03 · BGM 音量滑块真正生效**  
作为玩家，我想拖动设置面板里的 BGM 音量滑块（0-100），BGM 的音量实时改变，以便我控制背景音乐的响度而不影响游戏音效。  
- 滑块值 0-100 线性映射到 Howler `volume(0-1)`，实时生效（不需要关 Modal 才生效）。
- 滑块拉到 0 = BGM 静音（调用 `howl.volume(0)`，不 pause，保持循环位置）。
- 值持久化写 `localStorage.balatro.settings.bgmVolume`（第 1 轮已有此 key，沿用）。

**US-04 · SFX 音量滑块真正生效**  
作为玩家，我想拖动 SFX 音量滑块，所有操作音效（出牌、选牌、计分等）的响度统一跟着变，以便我可以单独把 BGM 调小但保留操作音效。  
- 同 BGM：0-100 映射到 0-1，实时生效；拉到 0 = 静音。
- SFX 音量影响所有非 BGM 的音效。

**US-05 · 出牌 / 弃牌核心音效**  
作为玩家，我想在点击"出牌"按钮时听到出牌音效，在点击"弃牌"按钮时听到弃牌音效，以便我确认操作已被响应。  
- 出牌音效：卡牌出手感（短促，<0.3s）。
- 弃牌音效：纸牌丢弃感（略低沉，<0.3s）。

**US-06 · 计分动画各步音效（对齐 §5.1 时序）**  
作为玩家，我想在 8 步计分动画的每个关键节点听到对应音效，以便声音和视觉动画互相强化节奏感。具体对应关系见第 5 节。

**US-07 · Joker 触发音效**  
作为玩家，我想在 Joker 上移金光的瞬间（动画触发起点）听到一个特殊音效，以便我感知到 Joker 的加成正在生效。

**US-08 · 通关 / 失败音效**  
作为玩家，我想在通关（`won` 状态）时听到胜利音效，在失败（`lost` 状态）时听到失败音效，以便结局有情绪感。  
- 通关/失败音效播放时，BGM 淡出（300-500ms fade）后停止。

### P1 · 重要（完整度）

**US-09 · 选牌 / 取消选牌音效**  
作为玩家，我想在点击手牌选中或取消选中时听到轻微音效，以便操作有即时反馈。  
- 选中音效和取消选中音效可以用同一个音效文件（短促 tick 声），也可以用两个不同音效。
- 注意：手牌高频快速选中时需要防止音效爆堆（见第 5 节性能要点）。

**US-10 · 商店买卡音效**  
作为玩家，我想在点击"购买 $X"成功购买 Joker 时听到购买音效，以便感知到"买到了"。

**US-11 · 按钮点击通用音效**  
作为玩家，我想在点击排序、跳过、重新开始等次要按钮时听到一个通用点击音效，以便所有按钮操作都有声音反馈。  
- 出牌、弃牌、购买按钮有专属音效（US-05/US-10），其余按钮用通用点击音效。

**US-12 · 发牌音效**  
作为玩家，我想在发牌动画（初始发牌/出牌后补牌/弃牌后补牌）时听到卡牌划过的声音，以便感知新牌到来。  
- 发牌是逐张错峰的（每张间隔 60ms），音效也逐张错峰触发，不能全部同时播。

### P2 · 锦上添花（有时间再做）

**US-13 · 计分爆出音效强化**  
作为玩家，我想在第 5 步"chips × mult = score"大字爆出时听到一个高潮音效（比普通计分音效更有冲击力），以便感知到本手得分的高潮点。

**US-14 · blindScore 累加时的音调变化**  
作为玩家，我想在 blindScore 从 0 数字插值跳动到目标分的过程中，听到音调随进度升高的提示音（类似 Balatro 原作的"咔咔咔"节奏声），以便感知分数积累的紧张感。  
- 实现成本较高，如时间不够可跳过。

---

## 5. 核心流程

### 5.1 音频初始化流程

```
页面加载
  → Howler.js 预加载所有音效文件（不播放）
  → 从 localStorage 读取 bgmVolume / sfxVolume 写入 Howler 实例
  → 等待"首次用户交互"（任意点击/键盘事件）
      → 触发 BGM 开始播放（loop: true）
  → 此后每次操作 → 触发对应 SFX
```

**浏览器 Autoplay 策略硬约束**：Chrome/Safari 等浏览器要求音频必须在用户手势事件（click、touchstart 等）的调用栈内才能播放。实现方式：监听全局首次 `click` 事件，在回调里调 `bgmHowl.play()`，之后移除监听器。不需要专门做"点击播放"按钮，利用游戏首次点击（选牌、出牌、任意按钮）即可触发。

### 5.2 计分动画各步音效对齐时序

对应第 1 轮 PRD §5.1 的 8 步时间线：

| 步骤 | 动画事件 | 音效触发时机 | 音效 ID |
|------|----------|-------------|--------|
| 1 | 选中牌飞向出牌区（350ms） | 飞牌动画开始瞬间 | `sfx_play_cards` |
| 2 | 显示牌型名 + chips/mult 初始值 | 牌型名出现瞬间 | `sfx_hand_type`（轻提示音）|
| 3 | 逐张高亮 + 蓝色 chips 飞字（每张 150ms） | 每张高亮起点（逐张错峰） | `sfx_chip_tick`（每张一次，共 1-5 次）|
| 4 | Joker 上移金光（每个 Joker 800ms） | 每个 Joker 动画起点 | `sfx_joker_trigger`（每个 Joker 一次）|
| 5 | 公式大字爆出（停留 800ms） | 大字出现瞬间 | `sfx_score_reveal` |
| 6 | blindScore 数字插值累加（600ms） | 累加开始瞬间 | `sfx_score_count`（低频持续或单次短音）|
| 7 | 出牌区淡出 + 新牌从牌堆飞入 | 每张新牌飞入起点（逐张错峰 60ms） | `sfx_deal_card`（每张一次）|

### 5.3 状态转移音效

| 状态转移 | 音效 |
|---------|------|
| `playing` → `shop`（通关一关） | `sfx_stage_clear` |
| `shop` → `playing`（跳过/买完进下一关） | `sfx_button_click` |
| `playing` → `won`（3 关全过） | `sfx_victory`，BGM 淡出 |
| `playing` → `lost`（手数用完） | `sfx_defeat`，BGM 淡出 |
| `won/lost` → `playing`（重新开始） | BGM 重新从头播放 |

### 5.4 音量实时映射

```
设置面板 BGM 滑块（0-100） → bgmHowl.volume(value / 100)
设置面板 SFX 滑块（0-100） → 全局 sfxVolume = value / 100（每次播 SFX 时传入）
```

两个值的改变都是 `input` 事件实时生效，不需要确认。

---

## 6. 数据与状态

### 已有（第 1 轮，不需要改）

- `localStorage.balatro.settings` 对象里的 `bgmVolume`（默认 50）和 `sfxVolume`（默认 70）字段已存在。
- 设置面板的两个 slider 已经绑定这两个值的读写。

### 本轮新增

- 无新增 localStorage 字段，沿用现有字段。
- 无后端、无账号体系，纯前端状态。
- Howler 实例建议作为模块级单例（`src/audio.js`），在 `App.vue` 挂载时初始化，不需要 Vuex/Pinia 状态管理。

### 音频文件

- 放在 `public/audio/` 目录下（Vite 的静态资源目录，构建后原样复制）。
- 格式：优先 `.mp3`，兼容性好，体积适中；如需更小体积可提供 `.ogg` 作为备选（Howler 支持格式数组自动选择）。
- 所有文件总体积目标：BGM 控制在 2-4 MB，SFX 全部合计控制在 500 KB 以内。

---

## 7. 需要的音效素材清单

以下是本轮需要准备的音频文件，全部采用 CC0 授权素材（freesound.org / OpenGameArt.org / Kenney 音效包）。

| 文件名 | 用途 | 建议时长 | 风格描述 | 参考来源 |
|--------|------|---------|---------|---------|
| `bgm_main.mp3` | 主循环 BGM | 60-120s（循环） | 轻松的复古像素风爵士/电子音乐，节奏中等偏快，无歌词，适合长时间循环不烦躁 | OpenGameArt.org 搜 "chiptune loop" 或 "pixel jazz" |
| `sfx_play_cards.mp3` | 出牌（牌飞出去） | 0.2-0.3s | 纸牌甩出的"嗖"声，干脆清脆 | freesound.org 搜 "card swipe" 或 "card throw" |
| `sfx_discard.mp3` | 弃牌 | 0.2-0.3s | 比出牌略低沉的纸牌丢弃声，可有轻微"扑"的质感 | freesound.org 搜 "card discard" |
| `sfx_card_select.mp3` | 选牌 / 取消选牌 | 0.05-0.1s | 极短的 tick 声或轻拨弦声，频繁触发不能烦 | Kenney UI Sound Pack 里的 click 类 |
| `sfx_hand_type.mp3` | 牌型名出现 | 0.1-0.2s | 轻柔的提示音（类似软提示 ding），表示"识别到牌型了" | freesound.org 搜 "soft ding" 或 "notification" |
| `sfx_chip_tick.mp3` | 逐张高亮 chips 累加 | 0.05-0.1s | 短促金属敲击感，类似计分器"咔"的一下，可连发 5 次不难听 | Kenney 或 freesound 搜 "coin tick" "register beep" |
| `sfx_joker_trigger.mp3` | Joker 触发金光 | 0.3-0.5s | 有魔法感的闪光音，带一点混响，神秘感 | freesound.org 搜 "magic sparkle" "power up" |
| `sfx_score_reveal.mp3` | 公式大字爆出 | 0.4-0.6s | 高潮爆发音，类似"砰"加上短混响，有冲击力 | freesound.org 搜 "score reveal" "impact boom" |
| `sfx_score_count.mp3` | blindScore 数字累加 | 0.2-0.4s | 低频嗡鸣或数字跳动感，单次触发即可 | freesound.org 搜 "score counting" |
| `sfx_deal_card.mp3` | 发牌（每张一次） | 0.1-0.2s | 纸牌滑入的轻微摩擦声，可以连续快速触发（每张 60ms 错峰） | freesound.org 搜 "card deal" "card slide" |
| `sfx_stage_clear.mp3` | 通过一关（进商店） | 0.5-1.0s | 轻快的胜利小段（3-5 个音符），不抢 BGM 风头 | OpenGameArt 搜 "stage clear jingle" |
| `sfx_buy_joker.mp3` | 商店购买 Joker | 0.3-0.5s | 硬币声 + 轻微叮咚，消费感 | freesound.org 搜 "coin purchase" "buy sound" |
| `sfx_button_click.mp3` | 通用按钮点击 | 0.05-0.1s | 通用 UI 点击声，中性短促 | Kenney UI Sound Pack |
| `sfx_victory.mp3` | 通关全部（won） | 2-4s | 完整的胜利旋律，欢快有成就感，可以是一个小段 jingle | OpenGameArt 搜 "victory fanfare chiptune" |
| `sfx_defeat.mp3` | 失败（lost） | 1-2s | 略带幽默或戏剧感的失败音效（下行音阶或"哎呀"感），不要太沉重 | freesound.org 搜 "game over" "failure jingle" |

**总计**：1 个 BGM + 14 个 SFX。

---

## 8. 性能要点

### 预加载策略

- 所有音频文件在 `audio.js` 模块初始化时统一预加载（`preload: true`），游戏开始前全部就绪，避免首次触发时的加载延迟。
- BGM 体积较大，可设置 `preload: 'metadata'` 先只加载元数据，首次交互后再触发实际加载 + 播放。

### 防止音效爆堆（同一音效高频重叠）

逐张高亮（US-06 步骤 3）和逐张发牌（US-12）会在极短时间内连续触发同一音效，需防止爆音：

- **方案**：每个可能高频触发的音效（`sfx_chip_tick`、`sfx_deal_card`、`sfx_card_select`），在 Howler 实例上设置 `pool` 参数（sound pool），允许同时存在多个播放实例，避免前一个还没播完就被截断。
- **限流兜底**：如果连续触发间隔 < 30ms，跳过这次触发（throttle），防止极端情况下爆音。
- **音量降低**：高频连发的音效（chip_tick、deal_card）本身音量相对其他音效低 10-15%，避免叠加后整体音量突增。

### 格式兼容

Howler 支持格式数组：`src: ['audio/sfx_play_cards.mp3', 'audio/sfx_play_cards.ogg']`，浏览器自动选择支持的格式。本轮只提供 mp3 也可以，主流浏览器都支持。

---

## 9. 不做什么

| 排除项 | 理由 |
|--------|------|
| 均衡器 / 音效滤镜（高低音、混响调节） | 过度复杂，玩家不需要 |
| 多套 BGM 切换（关卡主题曲、商店主题曲） | 素材制作成本高，单曲循环足够 |
| 音效自定义（玩家上传自己的音效） | 完全不在这个游戏的使用场景里 |
| 独立的"静音"开关按钮 | 拖滑块到 0 即静音，不需要额外按钮 |
| 空间音效 / 3D 音频 | 2D 游戏无必要 |
| 音频可视化（频谱图） | 与游戏玩法无关 |
| 粒子特效升级（通关撒金币、爆分粒子） | 原计划第 3 轮同批，但本轮用户明确只要音效，粒子特效单独排入第 3.5 轮或第 4 轮视情况补充 |
| 后台标签页时 BGM 静音 | 浏览器 Page Visibility API 处理，非核心，不做 |

> **关于粒子升级的说明**：第 1 轮 PRD §11.1 的 commit 规范写的是"第 3 轮 - 配乐 + 粒子升级"，但本次用户明确只要音效。**建议 commit message 调整为** `feat: 第 3 轮 - 全局音效 BGM + SFX（v1.2.0）`，粒子升级如需补充可在第 4 轮前作为 hotfix commit 或并入第 4 轮。

---

## 10. 成功标准

| 指标 | 验收标准 |
|------|---------|
| BGM 自动启动 | 用户首次点击任意交互后，BGM 在 200ms 内开始播放 |
| BGM 无缝循环 | BGM 循环衔接处无明显静音断点（≤100ms 间隙） |
| SFX 响应延迟 | 触发操作到听到音效 ≤ 50ms（预加载后） |
| 音量滑块实时生效 | 拖动滑块时，音量变化跟手（无肉眼可见延迟） |
| 音量持久化 | 刷新页面后 BGM/SFX 音量恢复为上次设置值 |
| 无爆音 | 快速连点选牌或触发逐张高亮，不出现爆音或明显失真 |
| 通关/失败音效 | 进入 won/lost 状态时，相应音效在 100ms 内播放，BGM 淡出 |
| 构建通过 | `npm run build` 0 error 0 warning |

---

## 11. 开放问题

1. **BGM 素材确认**：BGM 是开发者自行从 OpenGameArt 下载还是需要 AI 生成？如果用 AI 生成（如 Suno/Udio），CC0 授权需要确认。本 PRD 默认使用 CC0 手动下载。

2. **音效素材版本**：Kenney 音效包有多个版本（Interface Sounds、Game Sounds 等），具体下哪个包下游工程师自行判断，文件名对齐上面的清单即可。

3. **BGM 是否在 won/lost 后重启**：点"重新开始"后 BGM 是从当前位置继续还是从头播？建议从头播（给玩家新一局的仪式感），但无强烈偏好，工程师自行决定。

4. **移动端适配**：游戏第 1 轮没有明确做移动端，iOS Safari 的音频策略比桌面更严格（必须在 touchend 回调里播放）。本轮如果不考虑移动端，可忽略；如果需要，工程师按 Howler 官方的 Mobile Compatibility 文档处理。
